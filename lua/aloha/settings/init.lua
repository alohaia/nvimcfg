local settings = {}

settings.mappings = require('aloha.settings.mappings')
settings.options = require('aloha.settings.options')
settings.commands = require('aloha.settings.commands')

return settings
